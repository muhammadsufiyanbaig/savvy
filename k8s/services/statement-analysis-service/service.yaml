apiVersion: v1
kind: Service
metadata:
  name: statement-analysis-service
  namespace: savvy
spec:
  selector:
    app: statement-analysis-service
  ports:
  - port: 8004
    targetPort: 8004
    name: http
  type: ClusterIP
