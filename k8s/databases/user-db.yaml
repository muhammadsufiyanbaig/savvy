# user-db — staging only. Production: use RDS (set DATABASE_URL_USER in secret).
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db
  namespace: savvy
  labels:
    app: user-db
spec:
  serviceName: user-db
  replicas: 1
  selector:
    matchLabels:
      app: user-db
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: user_db
        - name: POSTGRES_USER
          value: savvy
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: savvy-secrets
              key: SECRET_KEY
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "300m"
        readinessProbe:
          exec:
            command: ["pg_isready", "-U", "savvy", "-d", "user_db"]
          initialDelaySeconds: 10
          periodSeconds: 5
        volumeMounts:
        - name: pgdata
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: pgdata
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: savvy
spec:
  selector:
    app: user-db
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None
